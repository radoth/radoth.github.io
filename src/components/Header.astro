---
import Hamburger from "./Hamburger.astro";
import Navigation from "./Navigation.astro";
import ThemeIcon from "./ThemeIcon.astro";
import { getImage } from "astro:assets";
const { is404Page = false } = Astro.props; // Get the prop from Astro.props

const favicon_avif = await getImage({
  src: "https://raw.githubusercontent.com/radoth/radoth.github.io/main/img/favicon.png",
  alt: "favicon",
  // inferSize: true,
  width: "500",
  height: "500",
  decoding: "async",
  format: "avif"
});
---

<header class="mb-[30px]">
  <nav
    class="flex items-center justify-between w-full h-full relative align-center"
  >
    {
      !is404Page && (
        <div>
          <a href="/">
            <img
              class="favicon absolute left-0 dark:invert -mt-[15px]"
              src={favicon_avif.src}
              alt="Home"
              width="30px"
              height="30px"
            />
          </a>
        </div>
      )
    }
    {
      !is404Page && (
        <div class="absolute right-[55px] -mb-[5px]">
          <ThemeIcon />
        </div>
      )
    }
    {
      !is404Page && (
        <div class="-mt-[3px]">
          <Hamburger />
        </div>
      )
    }
    {
      !is404Page && (
        <div class="absolute right-0">
          <Navigation />
        </div>
      )
    }
  </nav>
  <slot />
</header>
